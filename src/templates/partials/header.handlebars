<header>
  <a href="."><img src="{{ headerLogo }}"></a>
  <h1>{{ authorName }}</h1>
  <nav>
    <ul>
      {{#each nav}}
        <li><a href="{{ url }}">{{ page }}</a></li>
      {{/each}}
    </ul>
  </nav>
</header>
